const country = "Sweden";
const continent = "Europe";
let population = 10;
const isIsland = false;
let language;
language = "Swedish";
let halfPopulation = population / 2;
let incresePopulation = population + 1;
let finlandPopulation = 6;
let averagePopulation = 33;
const moreThanFinland = population > finlandPopulation;
const moreThanAverage = population > averagePopulation;
/* console.log(
  country,
  continent,
  population,
  isIsland,
  language,
  halfPopulation,
  incresePopulation,
  moreThanFinland,
  moreThanAverage
); */
/* const description = `${country} is in ${continent}, and its ${population} million people speak ${language}`;
console.log(description);

if (moreThanAverage) {
  console.log(`${country} has a population more than average`);
} else if (population === averagePopulation) {
  console.log(`${country} has a population of average`);
} else {
  console.log(`${country} has a population less than average`);
}


if (language === "Swedish" && population < 50 && !isIsland) {
  console.log(`jippi, sarah could live here`);
} else {
  console.log(`sarah dont want to live here`);
}
 */
/* 
const massMark = 108;
const heightMark = 1.69;

const massJohn = 78;
const heightJohn = 1.69;

const bmiMark = massMark / heightMark ** 2;
const bmiJohn = massJohn / heightJohn ** 2;
const markHigherBMI = bmiMark > bmiJohn

if(markHigherBMI) {
    console.log(`Marks bmi${bmiMark} is higher then Johns bmi${bmiJohn}`)
}

else if(bmiJohn === bmiMark) {
    console.log(`Marks bmi${bmiMark} is the same as then Johns bmi${bmiJohn}`)
}
else {
    console.log(`Marks bmi${bmiMark} is lower then Johns bmi${bmiJohn}`)
}
 */
/* 
const averageScoreDolphins = (0 + 10 + 1) / 2;
const averageScoreKoalas = (100 + 10 + 100) / 2;



if(averageScoreDolphins > averageScoreKoalas && averageScoreDolphins > 100) {
    console.log(`dolfins wins`)
}
else if(averageScoreDolphins < averageScoreKoalas && averageScoreKoalas > 100) {
    console.log(`koalas wins`)
}
else if(averageScoreDolphins === averageScoreKoalas && averageScoreKoalas > 100) {
    console.log(`both wins`)
}
else {
    console.log(`no wins`)
} */

/* const numNeighbours = prompt('how many?')


if(Number(numNeighbours) === 1) {
    console.log('only one')
}
else if(numNeighbours > 1) {
    console.log('more')
}
else {
    console.log('no neighbours')
} */

/* switch (language) {
  case "spanish":
    console.log("spanish");
    break;
  case "chinese":
  case "mandarin":
    console.log("chinese/mandarin");
    break;
  case "english":
    console.log("english");
    break;
  case "hindi":
    console.log("hindi");
    break;
  case "arabic":
    console.log("arabic");
    break;
    case "Swedish":
        console.log("swedish");
}

 */
/* 
console.log( `sweden poulation is ${ population > 33 ? 'more' : 'less'} than average`)
 */
/* const bill = 275;
let tip;
console.log(`the bill was ${bill}, the tip was ${tip = bill > 300 && bill < 50 ? bill * .15 : bill * .2 }, and the total value ${bill + tip}`) */